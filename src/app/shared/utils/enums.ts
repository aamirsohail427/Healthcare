
export enum PageType {
    List,
    Trowser,
    Modal
}

export enum DateSelection {
    WeekToDate,
    LastWeek,
    MTD,
    LastMonth,
    CurrMonth,
    QTD,
    LastQTR,
    YearToDate,
    LastYear,
    CurrYear,
    ThirMonths,
    ThirWeeks,
    Custom
}


export enum PageTemplateTypes {
  Page = 'Page',
  Header = 'Header',
  Footer = 'Footer'
}

export enum authStatus{
  failed = 1,
  success = 2
}